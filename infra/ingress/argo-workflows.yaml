apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argo-workflows
  namespace: argo
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`workflows.local`)
      kind: Rule
      services:
        - name: argo-workflows-server
          port: 2746
