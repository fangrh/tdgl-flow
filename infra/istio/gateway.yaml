apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: tdgl-gateway
  namespace: tdgl
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "tdgl.local"
        - "kubeflow.local"
